{
  "name": "breezybuddies",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BreezyBuddies — design preview for a one-product international DTC store.",
  "scripts": {
    "dev": "npx --yes serve . --listen 4321",
    "deploy": "npx --yes vercel --prod",
    "deploy:preview": "npx --yes vercel",
    "images": "python3 tools/build-images.py",
    "pages": "python3 tools/build-pages.py",
    "check": "node tools/check.mjs"
  }
}
